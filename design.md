# design.md — 디자인 결정

**대상** https://place-taxi.vercel.app · **기반** shadcn/ui + Tailwind CSS
**같이 쓰는 곳** `site/src/app/globals.css` (웹) · `theme.py` (Gradio 봇)

---

## 0. 왜 이 문서가 있나

처음 만든 화면은 shadcn 기본값 그대로였다. **전부 회색이었다.**
다른 조 결과물과 나란히 놓고서야 알았다 — 못생긴 게 아니라 **심심한** 것이었다.

| | 다른 조 A | 다른 조 B | 처음 내 것 |
|---|---|---|---|
| 색 | 파란 그라디언트 | 초록 | **회색뿐** |
| 단계 표시 | 1→2→3 | 1/2단계 | **없음** |
| 아이콘 | 🍽️🏨🏛️ | 🚕📍 | **없음** |
| 완료 화면 | 큰 확정 카드 | — | 작은 표 |

shadcn 이 회색인 건 미완성이라서가 아니다. **"브랜드 색은 네가 넣어라"** 는 뜻이다.
그걸 안 넣고 있었다. 이 문서는 그때 정한 것들을 적어둔 것이다.

---

## 1. 설정 (`components.json`)

```json
{
  "style": "base-nova",
  "baseColor": "neutral",
  "cssVariables": true,
  "iconLibrary": "lucide",
  "rsc": true
}
```

- **`cssVariables: true`** — 색을 CSS 변수로 둔다. 이래야 한 곳만 고쳐서 전체를 바꿀 수 있다
- **`baseColor: neutral`** — 바탕은 무채색으로 두고 **강조색만 따로 얹는다**.
  바탕까지 색을 넣으면 정작 강조해야 할 것(이월·완료)이 안 보인다

## 2. 쓴 컴포넌트 10개

```
badge  button  card  dialog  input  label  radio-group  select  separator  table
```

**직접 만든 것은 없다.** 전부 shadcn 에서 가져다 조합했다.
`site/src/components/ui/` 안에 파일로 들어와 있어서 필요하면 고칠 수 있다.

| 컴포넌트 | 어디에 |
|---|---|
| `card` | BLOCK 1(장소) · BLOCK 2(택시) · 배차 확정 |
| `select` | 지역 · 종류 · 가격대 |
| `radio-group` | 검색 결과 목록 · 차종 |
| `badge` | 상태 · **도착지 이월됨** · 고친 횟수 |
| `table` | 관리자 호출 기록 |
| `input` + `label` | 출발지 · 도착지 · 시간 · 비밀번호 |
| `separator` | 접수 / 수정 구분 |

---

## 3. 색 — 택시 옐로우 + 딥 네이비

```css
:root {
  --taxi:      #f5b301;   /* 택시 노랑 - 강조 · 진행중 */
  --taxi-deep: #d99700;
  --navy:      #0f172a;   /* 딥 네이비 - 히어로 · 기본 버튼 */
  --navy-2:    #1e293b;
  --ok:        #16a34a;   /* 초록 - 완료 */
  --carry:     #2563eb;   /* 파랑 - 이월 */
}
```

### 왜 이 색인가

**① 겹치지 않는다.** 다른 조가 파랑·초록이었다. 셋이 나란히 놓였을 때 구분된다.

**② 우기지 않아도 된다.** 택시는 원래 노랑이다. 설명이 필요 없는 색이다.

**③ 색마다 뜻이 하나씩이다.** 예쁘라고 쓴 색이 없다.

| 색 | 뜻 | 어디에 |
|---|---|---|
| 🟡 `--taxi` | **지금 여기** | 진행 중인 단계 · 선택된 도메인 탭 · 히어로 강조 |
| 🔵 `--carry` | **이월됐다** | 이월 안내 상자 · 도착지 입력칸 테두리 · `[도착지 이월됨]` 배지 |
| 🟢 `--ok` | **끝났다** | 배차 확정 카드 · 완료 배지 · 지나간 단계 |
| ⚫ `--navy` | 바탕 · 기본 버튼 | 히어로 그라디언트 · primary 버튼 |

**이월을 파란색 하나로 몰아둔 게 이 화면의 핵심이다.**
과제 체크리스트 2번이 "도착지가 이월되나요?" 인데,
파란 것만 눈으로 따라가면 이월이 어디서 일어났는지 바로 보인다.

```
🔵 도착지 자동 이월됨              ← 파란 안내 상자
도착지 [ 소문난 감자탕 ]           ← 파란 테두리
[도착지 이월됨]                    ← 파란 배지
관리자 표의 🔵 표시                ← 파란 점
```

---

## 4. 레이아웃

### 히어로 — 검증 체크리스트를 화면에 박았다

```
🚕 TAXI · 식당 · 숙소 · 관광
장소 접수 ➔ 택시 배차          ┌ 핵심 기능 검증 ───────┐
                              │ ✓ 두 블록 연계        │
[📋 일반 접수] [💬 챗봇 접수]  │ ✓ 도착지 자동 이월    │
                              │ ✓ 슬롯 자유 수정      │
                              └──────────────────────┘
```

**채점 기준을 화면에 그대로 띄웠다.** 보는 사람이 찾아다니지 않게 하려고.
다른 조 결과물에서 배운 것이다.

### 단계 표시 1 → 2 → 3

```
[1 장소 접수]  [2 택시 배차]  [3 배차 확정]
   노랑=진행중     회색=대기      초록=완료
```

지금 어디쯤인지 상태에 따라 저절로 바뀐다. 색이 곧 상태다.

### 두 칸 구조

```
┌─ BLOCK 1 🍽️ 장소 접수 ─┐  ┌─ BLOCK 2 🚕 택시 배차 ─┐
│ 도메인 · 조건 · 검색   │→ │ 🔵 이월 안내            │
│ 결과 목록 · 예약       │  │ 출발지 · 도착지 · 시간  │
└────────────────────────┘  └─────────────────────────┘
              ┌─ ✅ 배차 확정 TX-00012 ─┐
              │ 줄마다 [수정] 가능      │
              └─────────────────────────┘
```

카드 위쪽에 **색 띠**를 넣어 블록을 구분했다 (`.tab-place` 노랑 / `.tab-taxi` 파랑 / `.tab-done` 초록).

---

## 5. 수정을 두 가지로 만든 이유

강사 피드백 — *"장바구니처럼 변경할 수 있는 플로우를 생각해봐라"*

```
① 장바구니식   확정 카드에서 줄마다 [수정] → [저장]
② 폼 일괄식    위쪽 칸을 고치고 [변경 저장]
```

**둘 다 남긴 이유** — 9개 시나리오를 돌려보니 서로 이겼다.

| | 클릭 수 | 헷갈림 |
|---|---|---|
| 장바구니식 | 2 | **없음** (그 줄만 열림) |
| 폼 일괄식 | **1** | 옛 값을 기억해야 함 |

> **클릭 수는 폼이 이기고, 안 헷갈리기는 장바구니식이 이긴다.**

하나만 남기면 이 비교를 못 한다. 그래서 둘 다 뒀다.

---

## 6. 봇 화면 — 웹과 색을 맞췄다 (`theme.py`)

Gradio 는 React 가 아니라 shadcn 컴포넌트를 못 쓴다.
대신 **shadcn 이 쓰는 값을 그대로 베껴서** Gradio 테마로 옮겼다.

```python
THEME = gr.themes.Base(
    primary_hue=zinc, neutral_hue=zinc,
    radius_size=radius_md,            # 0.5rem
    font=[GoogleFont("Inter"), "Pretendard", ...],
).set(
    border_color_primary="#e4e4e7",   # shadcn zinc-200
    button_primary_background_fill="#18181b",
    ...
)
```

같은 테마를 쓰기 때문에 **웹 탭과 봇 탭을 오갈 때 이질감이 없다.**
그리고 비교가 공평해진다 — 디자인 차이가 아니라 **방식 차이**만 남는다.

### iframe 안에서 생긴 문제 둘

| 증상 | 원인 | 고침 |
|---|---|---|
| 봇 화면이 새까맣게 나옴 | Gradio 가 보는 사람 브라우저의 다크 설정을 따라감 | 주소에 **`?__theme=light`** |
| 아래에 검은 빈 공간이 길게 남음 | Gradio 컨테이너가 화면 높이를 다 먹음 | `html,body{height:auto}` · iframe 560px 고정 · 봇 탭에서 히어로 접기 |

---

## 7. 안 한 것

| | 왜 |
|---|---|
| 다크 모드 토글 | 색 변수는 다 잡아뒀지만 토글 UI 는 안 붙였다. 발표에 필요 없다 |
| 커스텀 폰트 | Next.js 기본 sans 를 그대로 썼다. 한글은 시스템 폰트가 제일 안전하다 |
| 애니메이션 | 상태가 색으로 이미 보인다. 움직임까지 넣으면 시끄러워진다 |
| 로고 | 이모지 🚕 하나로 대신했다 |

---

## 한 줄

> **색을 네 개만 쓰고, 그중 하나(파랑)를 '이월' 에만 썼다.
> 이 과제가 증명해야 하는 게 이월이기 때문이다.**
