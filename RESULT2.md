# 챌린지 2 - 대화만 했는데 데이터가 기록되나

**돌린 날** 2026-08-28 · **봇2** `bot2.py` (:7872) · **창고** Supabase `sessions`

돌린 방법 — `bot2.대화()` 에 사람 말을 그대로 넣고,
말이 끝날 때마다 창고를 **다른 곳에서 다시 읽어** 확인했습니다.
봇이 들고 있는 값이 아니라 창고에 적힌 값을 본 것입니다.

---

## 대화 3턴

| 턴 | 손님 | 창고에 새로 적힌 칸 | 5칸 중 |
|---|---|---|---|
| 1 | 스시 하나 예약해줘 | 장소-종류 `식당` · 장소-이름 `스시 하나` | 2 |
| 2 | **거기로** 가는 택시 불러줘 | 택시-도착지 `스시 하나` · **이월 true** | 3 |
| 3 | 강남역에서 19시에 출발할게요 | 택시-출발지 `강남역` · 택시-출발시간 `19:00` | **5 → 영수증** |

2턴이 핵심입니다. 손님은 **식당 이름을 다시 말하지 않았습니다.**
봇은 1턴을 하나도 기억하지 않는데, 창고에서 `스시 하나` 를 꺼내 와서 `거기` 를 풀었습니다.

3턴에서 마지막 칸이 차는 순간 `rides` 가 만들어지고 영수증이 나왔습니다.
호출번호 51 · 기사 김민수 · 배차완료.

---

## 주장 4개를 실제로 재봤습니다

| # | 주장 | 재는 법 | 결과 |
|---|---|---|---|
| 1 | 새로고침해도 남는다 | **다른 프로세스**에서 다시 읽음 | ✅ 5칸 그대로 |
| 2 | 입장 코드가 손님을 가른다 | 9번 손님 메모판을 읽음 | ✅ 전부 비어 있음 |
| 3 | 내 메모만 지워진다 | 9번을 지우고 테스트 손님을 봄 | ✅ 안 지워짐 |
| 4 | 어제 것과 구분된다 | `rides.source` 를 봄 | ✅ `bot2` |

1번이 어제 봇과 갈리는 지점입니다.
어제 봇은 슬롯이 `gr.State` 에 있어서 **봇 프로세스 밖에서는 읽을 수가 없었습니다.**
지금은 봇을 꺼도 값이 남아 있고, 웹사이트가 같은 것을 읽습니다.

---

## 턴을 1로 두면 뭐가 달라지나

프롬프트에 들어가는 것은 **슬롯 5칸 + 이번 말** 뿐입니다. 대화 기록은 안 넣습니다.

| | 대화 기록을 넣는 방식 | 이 방식 |
|---|---|---|
| 1턴 프롬프트 | 1배 | 1배 |
| 20턴 프롬프트 | **20배** | **1배** |
| 봇이 기억하는 것 | 전부 | **없음** |
| 기억하는 것 | 봇 | **창고** |

`turns` 칸에 몇 번 말했는지 쌓이지만, 그것도 창고에 적힐 뿐 프롬프트에는 안 들어갑니다.

---

## 아직 안 해본 것

- 실제 브라우저로 켜서 눌러본 것은 아직입니다. 함수를 직접 부른 결과입니다.
- `SHARE=1` 로 공개 주소를 만들어 웹 `/bot2` 에서 확인하는 것은 남았습니다.
- 슬롯을 다 채운 뒤 **고치는** 경우(어제 시나리오 9개 같은 것)는 안 재봤습니다.
  지금은 영수증이 한 번 나오면 `이미 영수증이 나왔습니다` 로 끝납니다.

---

# 브라우저로 눌러보고 나서 (같은 날 오후)

함수만 부를 때는 안 보이던 것들이 나왔습니다. 8개를 고쳤습니다.

| # | 무엇이 잘못됐나 | 어떻게 고쳤나 |
|---|---|---|
| 1 | 입장 코드가 웹·봇에 하나씩 있어 따로 놀았다 | 웹이 `?code=` 로 넘기고 봇은 읽기만 |
| 2 | 봇 안에도 영수증표가 있어 화면이 세로로 길어졌다 | 한 줄 상태로. 영수증은 웹이 그림 |
| 3 | 창고에 없는 장소가 그대로 저장됐다 | 저장 안 하고 있는 곳을 안내 |
| 4 | 3을 막았더니 같은 이름이 도착지로 새어 들어갔다 | 도착지·출발지에 같은 글자면 같이 버림 |
| 5 | 영수증이 나온 뒤 슬롯만 바뀌어 값이 어긋났다 | 호출도 같이 고침 (`update_ride`) |
| 6 | `demo.load` 에 `api_name=False` 를 안 붙여 HTTP 500 | 붙임 |
| 7 | `바삭치킨` 이 `바삭 치킨` 과 안 맞았다 | 공백을 없애고 비교 |
| 8 | 봇이 방금 물은 칸으로 답이 안 들어갔다 | 아래 참고 |

## 8번이 이 과제의 진짜 시험이었습니다

```
봇: 어디서 타시겠어요?      손님: 경복궁   ->  장소-이름이 바뀜   (틀림)
봇: 어디로 가시나요?        손님: 신내동   ->  출발지에 들어감    (틀림)
```

봇이 방금 무엇을 물었는지 모르기 때문입니다. **턴을 1로 한 대가**입니다.

대화 기록을 넣어서 풀지 않았습니다.
봇은 늘 **빈칸 중 첫 번째**를 되묻기 때문에, 창고의 슬롯만 보면
직전에 무엇을 물었는지 **다시 계산할 수 있습니다.**
그 한 줄을 프롬프트에 넣으니 제자리에 들어갔습니다.
기억을 늘린 것이 아니라 창고에서 되살린 것입니다.

## 짐작하지 말고 되묻기

```
"배고프네요"  ->  장소-종류 = 식당      (봇이 짐작)
"7시"        ->  19:00                 (봇이 짐작. 아침에 가려던 사람이 저녁 예약을 받음)
```

짐작한 것 자체보다 **짐작해놓고 티를 안 낸 것**이 문제였습니다.
손님이 친 글자가 값 안에 그대로 있으면 들은 것, 없으면 짐작입니다.
이 구분은 모델에 더 안 물어도 글자만 대보면 압니다.

이제 짐작한 칸은 **적지 않고 되묻습니다.**

| 턴 | 말 | 봇 | Gemini |
|---|---|---|---|
| 1 | 배고프네요 | 장소-종류 **식당** 으로 봐도 될까요? | 1콜 |
| 2 | 네 | 적었습니다 — 장소-종류 **식당** | **0콜** |
| … | | | |
| 6 | 7시 | **오전 07:00** 인가요, **오후 19:00** 인가요? | 1콜 |
| 7 | 아침이요 | 적었습니다 — 택시-출발시간 **07:00** → 🧾 | **0콜** |

**7턴에 5콜.** 턴은 늘었는데 모델 호출은 늘지 않았습니다.
되묻기의 답은 규칙으로 풀 수 있어서 모델을 부를 일이 없기 때문입니다.

무엇을 물었는지도 창고 `pending` 칸에 적습니다.
봇은 다음 턴에 아무것도 기억하지 못하니까요. 슬롯을 창고에 둔 것과 같은 이야기입니다.
