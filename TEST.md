# 테스트 시나리오 9개 - 직접 돌리는 법

**웹** https://restaurant-taxi.vercel.app  ·  **봇** /bot 탭

## 시작 전에 딱 한 번

Supabase → SQL Editor → `schema.sql` 전체 붙여넣고 **Run**.
장소 표가 `restaurants` 에서 `places` 로 바뀌었고 숙소·관광이 들어갔습니다. 안 하면 화면이 빕니다.

확인 — `./.venv/bin/python check_db.py` 에서 이 줄이 나와야 합니다.

```
장소 OK  18곳  (관광 5 · ں당 8 · 숙소 5)
```

## 재는 법

| 재는 것 | 어떻게 |
|---|---|
| **클릭 수** | 마우스로 누른 횟수. 글자 치는 건 안 셈 |
| **되나?** | O = 원하는 대로 됨 / △ = 되는데 돌아감 / X = 안 됨 |
| **이상한 점** | 헷갈렸던 순간을 한 줄로. **이게 제일 값진 칸입니다** |

> 잘 되는 것보다 **막히는 것**을 적으세요. 발표거리는 거기서 나옵니다.

---

# 장소를 바꾼다

## 1. "헬스장 있는 숙소요" → "아 헬스장 없어도 돼요"

```
① 🏨 숙소 탭
② [헬스장] 칩 켜기          ← 파랗게 변함
③ [숙소 검색]               → 3곳
④ [헬스장] 칩 끄기          ← 이게 "없어도 돼요"
⑤ [숙소 검색]               → 5곳
```

**볼 것** — 조건을 *푸는* 게 조건을 *거는* 것만큼 쉬운가?

## 2. "청와대로 정할게요" → (택시까지) → "아니 경복궁으로"

```
① 🏛️ 관광 탭 → [관광 검색] → 청와대 고르기 → [이 장소 예약]
② 도착지에 "청와대" 가 저절로 들어감          ← 이월
③ 출발지 "강남역", 시간 "14:00" → [택시 부르기]
④ 다시 [관광 검색] → 경복궁 고르기 → [이 장소 예약]
```

**볼 것** — ④에서 도착지랑 이미 부른 택시가 **같이** 바뀌나?

## 3. "치킨집 찾아요" → "그냥 일식집으로요"

```
① 🍽️ 식당 탭 → 종류 [치킨] → [식당 검색]   → 2곳
② 종류 [일식] → [식당 검색]                → 2곳
③ 하나 고르고 [이 장소 예약]
```

---

# 택시를 바꾼다

## 4. "4시 30분 출발" → "20시 30분으로"

```
① 장소 예약 → 출발지 "강남역", 시간 "04:30" → [택시 부르기]
② 아래 확정 카드에서 출발시간 줄의 [수정]
③ 20:30 으로 고치고 [저장]
```

**두 가지 길이 있습니다.** 둘 다 해보고 클릭 수를 비교하세요.
- 장바구니식 — 확정 카드에서 그 줄만 [수정]
- 폼 일괄식 — 위쪽 칸 고치고 [변경 저장]

## 5. "아무 택시나요" → "고급 택시로 해주세요"

```
① 차종 [아무거나] 로 [택시 부르기]     → 기사 김민수(일반)
② 확정 카드에서 차종 줄 [수정] → 고급 → [저장]
③ 기사가 이창호(고급) 로 바뀌는지 확인
```

**볼 것** — 차종만 바꿨는데 **기사까지 자동으로 바뀌나?**

## 6. "엄중식이라는 식당에서 출발" → "그냥 집에서 출발"

```
① 🍽️ 식당 탭 → 검색 → 엄중식 예약
② 출발지에 "엄중식" 이라고 직접 치기 → [택시 부르기]
③ 확정 카드 출발지 [수정] → "집" → [저장]
```

**함정** — 엄중식은 **출발지**입니다. 도착지에 이월된 것과 헷갈리지 않는지 보세요.

---

# 둘 다 바꾼다

## 7. ⭐ "경복궁 → 창덕궁" — 도착지도 따라가나?

**강사님이 핵심이라고 한 문제. 정답이 없습니다.**

```
① 🏛️ 관광 탭 → 검색 → 경복궁 예약
② 도착지 = 경복궁 (자동)
③ 출발지 "서울역", 시간 "10:00" → [택시 부르기]
④ 다시 [관광 검색] → 창덕궁 → [이 장소 예약]
```

**우리 앱이 고른 답** — 도착지가 **창덕궁으로 자동으로 따라갑니다.**
확정 카드에 `[도착지 이월됨]` 배지가 그대로 붙어 있고 고친 횟수가 올라갑니다.

**적을 것** — 이게 맞는 설계인가? 반대 의견도 있습니다.

| 자동으로 따라감 (우리) | 물어봄 |
|---|---|
| 손님이 다시 안 쳐도 됨 | 손님이 도착지만 경복궁으로 남기고 싶었을 수도 |
| 클릭이 적음 | 멋대로 바꾼 것처럼 느껴질 수 있음 |

**네가 어느 쪽이 맞다고 보는지, 왜 그런지 한 줄 적으면 그게 발표의 결론입니다.**

## 8. "숙소 A + 4시" → "숙소 B + 6시"

```
① 🏨 숙소 탭 → 검색 → 그랜드 호텔 예약
② 시간 "04:00" → [택시 부르기]
③ 다시 [숙소 검색] → 시티 호텔 → [이 장소 예약]   ← 장소 바뀜
④ 시간을 06:00 으로                              ← 시간 바뀜
```

**두 가지 길을 꼭 비교하세요.**
- ③④를 따로 (장바구니식) → 클릭 몇 번?
- 위쪽 폼에서 시간 고치고 [변경 저장] → 몇 번?

## 9. 다 정한 뒤 → "처음부터 다시요. 식당으로 할게요"

```
① 아무 도메인이나 끝까지 진행 (예약 + 택시 부르기)
② [✨ 처음부터 다시]
③ 🍽️ 식당 탭 → 새로 진행
```

**볼 것** — 앞서 부른 택시는 어떻게 되나? (우리 앱은 `취소` 로 바뀝니다)
표에서 그 줄의 상태를 확인하세요.

---

# 적는 표 — 이대로 채우면 됩니다

## 웹 (실물)

| # | 시나리오 | 되나? | 클릭 | 어느 길이 빨랐나 | 이상한 점 |
|---|---|---|---|---|---|
| 1 | 헬스장 켰다 껐다 | | | — | |
| 2 | 청와대 → 경복궁 | | | — | |
| 3 | 치킨 → 일식 | | | — | |
| 4 | 4:30 → 20:30 | | | 장바구니 / 폼 | |
| 5 | 아무거나 → 고급 | | | 장바구니 / 폼 | |
| 6 | 엄중식 → 집 | | | 장바구니 / 폼 | |
| 7 | ⭐ 경복궁 → 창덕궁 | | | — | |
| 8 | 숙소 A+4시 → B+6시 | | | 장바구니 / 폼 | |
| 9 | 처음부터 다시 | | | — | |

## Flow Diagram (그림)

**진짜 물어보는 건 이겁니다 — 내가 그린 그림에 그 길이 있나?**

| # | 이 길이 그림에 있나 | 없으면 어디에 그려야 하나 |
|---|---|---|
| 1 | 조건을 *푸는* 화살표 | |
| 2 | 장소 재선택 → 도착지 갱신 | |
| 3 | 검색 → 재검색 되돌아가는 화살표 | |
| 4~6 | 배차 확정 뒤 슬롯 수정 화살표 | |
| 7 | ⭐ 장소 변경 → 택시 도착지 갱신 | |
| 8 | 두 블록을 동시에 고치는 길 | |
| 9 | 전체 초기화 화살표 | |

> 대부분의 Flow Diagram은 **한 방향으로만** 흘러갑니다.
> 9개는 전부 **되돌아가는** 길이에요. 그림에 그 화살표가 없으면 **그게 발견입니다.**

---

# 마지막 - 비교표

세 방식을 나란히 놓으세요.

| 항목 | 웹 · 장바구니식 | 웹 · 폼 일괄식 | 봇 (대화) |
|---|---|---|---|
| 한 칸만 바꿀 때 | | | |
| 여러 칸 한 번에 | | | |
| 장소를 바꿀 때 | | | |
| 헷갈린 적 | | | |
| 추천 상황 | | | |

**한 줄 결론:**
